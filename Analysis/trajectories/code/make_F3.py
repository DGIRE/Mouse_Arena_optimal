"""Build figure F3 (angle vs distance-to-source) for Hypothesis 2.

STRICTLY from data/stats.json (H2 block). No science recomputed.
Outputs: reports/figures/F3_angle_vs_distance.{png,pdf,txt}
"""
import json
import os

import matplotlib
matplotlib.use("Agg")  # headless
import matplotlib.pyplot as plt

BASE = r"C:\Projects\Repos\Mouse Arena\Analysis\trajectories"
STATS_PATH = os.path.join(BASE, "data", "stats.json")
FIG_DIR = os.path.join(BASE, "reports", "figures")
STEM = os.path.join(FIG_DIR, "F3_angle_vs_distance")

os.makedirs(FIG_DIR, exist_ok=True)

with open(STATS_PATH, "r", encoding="utf-8") as f:
    stats = json.load(f)

h2 = stats["H2"]
curve = h2["F3_binned_curve"]

x = curve["bin_centers"]
y = curve["mean_theta"]
lo = curve["ci_lo"]
hi = curve["ci_hi"]
n_trials = curve["n_trials"]
n_samples = curve["n_samples"]
bin_width = curve["bin_width_px"]
min_samples = curve["min_samples_per_bin"]
dist_track = curve["distance_track"]

wil = h2["wilcoxon_slopes_gt_0"]
wil_stat = wil["stat"]
wil_p = wil["p"]
median_slope = h2["median_slope_deg_per_px"]
slope_ci = h2["median_slope_ci95_boot"]
n_trials_slope = h2["n_trials_with_finite_slope"]
pool = h2["pool"]
seed = stats["meta"]["seed"]

# ---- plot ----
fig, ax = plt.subplots(figsize=(7.5, 5.0))

ax.fill_between(x, lo, hi, color="#4C72B0", alpha=0.22,
                label="95% CI (bootstrap over trials)")
ax.plot(x, y, color="#1f4e8c", lw=2.0, marker="o", ms=4.0,
        label="Mean $\\theta$ (pooled %s)" % pool)

ax.set_xlabel("Distance to source $d$ (px, %g-px bins, %s track)"
              % (bin_width, dist_track))
ax.set_ylabel("Mean absolute body-axis-to-source angle $\\theta$ (deg)")
ax.set_title("F3 - Alignment vs distance to source (H2)")

# Annotation: H2 per-trial-slope Wilcoxon result. Direction before significance.
annot = (
    "H2: per-trial OLS slope of $\\theta$ on $d$ (unit = trial, n = %d)\n"
    "Direction: POSITIVE (median slope = %+.4f deg/px,\n"
    "  95%% boot CI [%+.4f, %+.4f])\n"
    "Wilcoxon signed-rank (slopes > 0): W = %.1f, p = %.3f\n"
    "NOT significant at alpha = 0.05 -> H2 NOT supported\n"
    "(trend is in the predicted direction)"
    % (n_trials_slope, median_slope, slope_ci[0], slope_ci[1],
       wil_stat, wil_p)
)
ax.text(0.98, 0.03, annot, transform=ax.transAxes, ha="right", va="bottom",
        fontsize=8, family="monospace",
        bbox=dict(boxstyle="round", fc="#fff8e1", ec="#c9a227", alpha=0.95))

ax.legend(loc="upper right", fontsize=8, framealpha=0.9)
ax.grid(True, alpha=0.25)
fig.tight_layout()

png_path = STEM + ".png"
pdf_path = STEM + ".pdf"
fig.savefig(png_path, dpi=200, bbox_inches="tight")
fig.savefig(pdf_path, bbox_inches="tight")
plt.close(fig)

# ---- .txt sidecar: source file + every number drawn ----
lines = []
lines.append("F3_angle_vs_distance - sidecar (numbers drawn in the figure)")
lines.append("Source: %s (H2 block)" % STATS_PATH)
lines.append("Generated by: code/make_F3.py")
lines.append("Pool: %s ; distance track: %s ; bin width: %g px ; "
             "min samples/bin: %d ; bootstrap seed: %d"
             % (pool, dist_track, bin_width, min_samples, seed))
lines.append("")
lines.append("H2 per-trial slope summary (theta ~ d OLS per trial, unit=trial):")
lines.append("  n trials with finite slope: %d" % n_trials_slope)
lines.append("  direction: %s" % h2["direction"])
lines.append("  median slope: %+.6f deg/px" % median_slope)
lines.append("  median slope 95%% boot CI: [%+.6f, %+.6f]"
             % (slope_ci[0], slope_ci[1]))
lines.append("  Wilcoxon signed-rank (alternative='greater', slopes>0): "
             "W=%.1f, p=%.6f" % (wil_stat, wil_p))
lines.append("  -> NOT significant at alpha=0.05; trend in predicted direction.")
lines.append("")
lines.append("F3 binned curve (x=bin_center_px, y=mean_theta_deg, "
             "ci_lo, ci_hi, n_trials, n_samples):")
hdr = "  %10s  %12s  %10s  %10s  %9s  %10s" % (
    "d_px", "mean_theta", "ci_lo", "ci_hi", "n_trials", "n_samples")
lines.append(hdr)
for i in range(len(x)):
    lines.append("  %10.1f  %12.6f  %10.6f  %10.6f  %9d  %10d" % (
        x[i], y[i], lo[i], hi[i], n_trials[i], n_samples[i]))
lines.append("")
# Describe whether the curve rises with distance (from stored curve)
first_y, last_y = y[0], y[-1]
ymin_i = min(range(len(y)), key=lambda i: y[i])
ymax_i = max(range(len(y)), key=lambda i: y[i])
lines.append("Curve shape (from stored values): first bin theta=%.2f deg at d=%.0f px; "
             "last bin theta=%.2f deg at d=%.0f px."
             % (first_y, x[0], last_y, x[-1]))
lines.append("  min theta=%.2f deg at d=%.0f px ; max theta=%.2f deg at d=%.0f px."
             % (y[ymin_i], x[ymin_i], y[ymax_i], x[ymax_i]))
lines.append("  The binned curve is non-monotonic (does NOT rise steadily with "
             "distance): H2's requirement that the F3 curve rises with distance "
             "is not clearly met.")

with open(STEM + ".txt", "w", encoding="utf-8") as f:
    f.write("\n".join(lines) + "\n")

print("WROTE:", png_path)
print("WROTE:", pdf_path)
print("WROTE:", STEM + ".txt")
print("png bytes:", os.path.getsize(png_path))
print("pdf bytes:", os.path.getsize(pdf_path))
print("txt bytes:", os.path.getsize(STEM + ".txt"))
