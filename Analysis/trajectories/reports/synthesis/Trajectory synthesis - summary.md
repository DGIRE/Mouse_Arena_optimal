# Trajectory Structure & Odor-Guided Orientation — Synthesis Report

**Mouse Arena · `Analysis\trajectories` · single infrared cohort · pooled Loc1–Loc6, n = 105 trials (unit = trial)**
Synthesizes `H1 – encounters vs tortuosity`, `H2 – alignment vs distance`, `H3 – peri-contact reorientation`, and the Gate-2 audit. Prepared 2026-08-08.

## Take-home findings

**Bottom line.** Across this cohort, **none of the three predicted odor-guided navigation effects is supported.** The deeper reason is a data reality: **discrete, high-amplitude ethanol encounters are essentially absent (~0–1 per trial)** at any physically meaningful signal level, so every hypothesis that hinges on counting or time-locking to "encounters" is underpowered by the measurement itself, not merely by the statistics. The one odor–behavior relationship that *does* replicate is weak and indirect: **straighter paths spend a larger fraction of their length in above-threshold odor.**

1. **H1 — more encounters ⇒ straighter search: NOT supported.** The raw correlation is strong but *positive* (Spearman ρ = 0.612, 95% CI [0.454, 0.736], p = 3.9e-12) — the opposite of the prediction — and is a **path-length exposure artifact**: both encounter count and tortuosity scale with path length (ρ = 0.71 and 0.93). Once exposure is removed, the encounter–tortuosity link **collapses to null** (encounter-rate vs tortuosity ρ = 0.077, p = 0.44, CI spans 0).

2. **H2 — body axis aligns as the animal nears the source: NOT supported.** Per-trial slopes lean in the predicted direction (median +0.020°/px, 64 positive vs 41 negative) but are **not significant** (Wilcoxon p = 0.12), and the distance–angle curve is **non-monotonic** — best alignment occurs at *intermediate* distances (θ ≈ 45° near 230–290 px), not closest to the port (θ ≈ 64° at 10 px). The "rises steadily with distance" acceptance criterion fails.

3. **H3 — reorientation toward the source after a contact: NULL.** Pre- vs post-contact angle is unchanged (median 70.0° → 69.7°; paired Wilcoxon p = 0.998), and if anything the angle *increases* slightly after contact (majority 64/41 post > pre) — no turn toward the source.

4. **The measurement, not just the result, is the story.** The calibrated encounter detector fired at the **deconvolved noise floor** (threshold 7.2e-4 a.u.; median signal at detected onsets 7.5e-4 a.u.; real odor excursions reach ≈ 0.14 a.u.). Fewer than 0.03 % of the 7,814 detected onsets exceed 0.01 a.u.; at a physically meaningful threshold the count is **~0–1 encounters per trial**. The "≈ 28 encounters/trial" figure indexes low-level fluctuation/exposure crossings, not discrete odor hits.

| Hypothesis | Predicted | Observed | Verdict |
|---|---|---|---|
| H1 encounters → less tortuous | negative ρ | raw ρ = +0.612 (exposure artifact); normalized ρ = +0.077, p = 0.44 | Not supported |
| H2 alignment improves when closer | slopes > 0; curve rises with distance | median slope +0.020°/px, p = 0.12; curve non-monotonic | Not supported |
| H3 reorient toward source post-contact | post θ < pre θ | 70.0° → 69.7°, p = 0.998 (slightly post > pre) | Null |
| *Secondary:* frac-of-path in odor vs tortuosity | (negative, exploratory) | ρ = −0.419, p = 8.7e-6 (replicates prior −0.53) | Only supported odor–behavior link |

```{=openxml}
<w:p><w:r><w:br w:type="page"/></w:r></w:p>
```

## What was tested

Three hypotheses about odor-guided navigation were evaluated on the head-mounted-sensor behavior cohort (114 trials; pooled Loc1–Loc6 = 105; `anotherLoc` = 9 held out). Geometry was computed on the cleaned body-centroid track; the body axis was defined as `unit(head − body)` and the angle to source θ as the absolute angle to `unit(endpoint − body)`, θ ∈ [0,180°]. Ethanol was read as the deconvolved sensor trace (`ethdeconv`, a.u.) interpolated onto the ~99 Hz head clock. Replication unit = trial; seed 1234; all reported numbers injected from saved result objects. This synthesis draws only on the three hypothesis reports and the auditor's independent re-derivation.

## Findings in detail

**H1 — encounters vs tortuosity.** The naive result looks like a strong effect in the wrong direction (ρ = +0.612). The auditor and the H1 report both trace this to exposure: longer trials accumulate both more threshold crossings and more accumulated turning, so path length drives both variables (ρ with path length = 0.71 for encounters, 0.93 for tortuosity). The exposure-normalized encounter rate is unrelated to tortuosity (ρ = 0.077, p = 0.44). Every one of the six locations shows the same positive raw sign, consistent with a shared exposure driver rather than a location-specific mechanism. The single measure that behaves as the hypothesis' *spirit* predicts is the **fraction of the path spent above the odor threshold**, which is negatively associated with tortuosity (ρ = −0.419, p = 8.7e-6) and reproduces the earlier Plume-locations value (ρ = −0.53) — straighter, shorter paths spend proportionally more of their length in odor. This is exposure-insensitive and is the most defensible odor–behavior signal in the dataset, though it is a proportion, not the encounter count H1 was framed on.

**H2 — alignment vs distance.** Direction is as predicted (positive per-trial slope of θ on distance: larger angle when farther, better alignment when nearer), but the population test is not significant (W = 3151, p = 0.12), and the pooled distance–angle curve is the more interesting object: it is **non-monotonic**, dropping to its minimum (~45°) at intermediate distances (≈ 230–290 px) and rising again both very near the port (~64° at 10 px) and far away (~80° at the sparsely sampled 530 px bin). A linear-slope test is the wrong lens for a U-shaped relationship; the animals are, if anything, *best* aligned during mid-range approach rather than in the immediate vicinity of the port (where local search and port-directed micro-movements likely dominate).

**H3 — peri-contact reorientation.** The peri-contact angle curve is essentially flat and drifts slightly *upward* across contact onset (≈ 71.9° at t = 0 rising to ≈ 73.6° by +0.5 s), the opposite of a turn toward the source; the trial-level paired test is decisively non-significant (p = 0.998). Because the triggering "contacts" are noise-floor crossings rather than discrete strong-odor events (below), a flat peri-event average is the expected outcome — there is no salient stimulus to react to.

**The encounter-detection problem (cross-cutting).** This is the most important methodological takeaway. The FPR-calibrated detector, following the "smallest-k passing" rule on a near-silent quiet baseline, selected the most permissive threshold (k = 5), which lands at the trace's own noise floor. The result is ≈ 28 "encounters"/trial that are statistically real crossings but biologically meaningless as odor hits. At a threshold matched to genuine signal excursions (0.005–0.01 a.u.) the count falls to ~0–1/trial — matching the original expectation. This does not invalidate the conclusions (all encounter-dependent results are null, and the raw H1 positive is correctly attributed to exposure), but it means the head-sensor cohort, as aggregated, **does not contain a usable population of discrete ethanol encounters** for encounter-triggered analyses.

## Caveats

The encounter counts are noise-floor crossings, not discrete odor contacts, so any encounter-count or peri-contact result is limited by the near-absence of strong-odor events (~0–1/trial at a physical threshold). The ethanol signal is uncalibrated (arbitrary units, not ppm) and spatial units are pixels (not calibrated to cm), so magnitudes and distances are within-cohort only. Statistics use trial as the replication unit with no animal-level grouping, so trials from the same mouse are treated as independent — a pseudoreplication risk (see extensions). Distance-to-source is geometrically confounded: all six reward ports sit toward the right/upper arena (x ≈ 440–510), so "distance to source" co-varies with absolute arena position and wall proximity, and no positional or edge control was applied. The body axis is a proxy (`head − body`); frames with a very short head–body separation are dropped and a velocity-based heading was not tested as an alternative. H2's linear-slope test cannot capture the observed non-monotonic curve, so its non-significance should not be read as "no relationship." No family-wise correction was applied across H1–H3 (disclosed; per-test CIs). Finally, this is a single infrared cohort with no cross-cohort or cross-modality replication.

## Suggested extended analyses

The most valuable next step is to **stop relying on discrete encounter counts** and instead model **continuous odor exposure and steering**. Concretely, replace the threshold detector with (a) integrated or instantaneous `ethdeconv` (or the raw `ethanol` trace against the per-trial `threshold` attribute) as a continuous predictor, and (b) regress instantaneous turning rate / angular velocity toward the source on instantaneous odor concentration and its temporal gradient (a casting-vs-surging test). This tests reactive steering directly, without needing a usable population of discrete "encounters," and sidesteps the noise-floor problem entirely.

For H2, model θ(distance) with a spline or GAM, or test alignment within near / mid / far distance zones, to characterize the non-monotonic curve rather than forcing a single linear slope; separating an explicit approach phase from local search near the port would sharpen this. A **shuffled-source null** — recompute alignment after assigning each trial a randomly chosen *other* Loc endpoint — would establish whether any alignment exceeds the geometric baseline expected from arena layout, addressing the position confound.

A concrete opportunity the run did not exploit: the `file_name` fields carry a **mouse identifier** (e.g. `…-205_Trial1_Loc5`, i.e. animals 201/204/205/…) and a date. The pipeline treated animal as unavailable (the standing rule expected a *6-digit* token), but this 3-digit ID is parseable. Recovering it enables **animal- and session-level mixed-effects models** (random intercepts for animal/session) that properly address pseudoreplication, plus learning-over-session and per-animal-consistency analyses. This is worth verifying against the raw metadata before relying on it.

Two further robustness moves: cross-check tortuosity and θ computed on the **body vs head track** (and a velocity-based heading) to confirm the geometry choices don't drive the nulls; and run a **positive-control simulation** — a synthetic agent that genuinely steers up-gradient — through the same pipeline to confirm it *would* detect alignment/reorientation if present, converting these nulls from "no effect found" into "no effect, and the method is sensitive." Finally, the per-Loc **odor-field maps already built in the Plume-locations analysis** could be joined to trajectories to relate each animal's instantaneous position-in-the-mapped-field (and local gradient bearing) to its steering — a spatially explicit test of gradient following that reuses existing artifacts.

## Status & provenance

All three hypothesis analyses passed **Gate 1 (code)** and **Gate 2 (independent science audit)** after one documentation-only revision; the auditor re-derived every headline number from the accessor and stored objects and confirmed the de-jump (0.4 %/1.0 % of body/head samples removed — well under the 5 % cascade smell test), clock alignment, angle convention (near-source θ ≈ 22°), exposure-confound handling, and honest null reporting. Sources: `reports\H1 – encounters vs tortuosity.docx`, `reports\H2 – alignment vs distance.docx`, `reports\H3 – peri-contact reorientation.docx`, `reports\audit_report.md`, and figures `F1–F4` (with `.txt` sidecars). Underlying results: `data\stats.json`, `data\trajectory_metrics.{json,h5}`; seed 1234; `traj_common/1.0`.
